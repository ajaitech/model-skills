Project: aivedha-main-site — Vite 6 + React 19 + TypeScript static marketing/legal site, served in production by an Express 4 static file server on Google Cloud Run. No database, no cloud SDK, no payments, no auth, no application API. Design is "Editorial Luxe", NOT Liquid Glass — do not fetch the design skill.

| Condition | Skill URL |
|---|---|
| Any work on the Vite/React/TypeScript frontend — components, routing, Tailwind v4 styling, `vite.config.ts`, `tsconfig.json`, the build | https://raw.githubusercontent.com/ajaitech/model-skills/main/tech/web-typescript-react-vite.md |
| Ad-hoc machine or system task, or GCP billing/quota/support via `gcloud` — not product code | https://raw.githubusercontent.com/ajaitech/model-skills/main/machine/MACHINE_INDEX.md |

Fetch only rows whose condition is true right now.

Deliberately absent, re-verified in source: `design-liquid-glass` — the site is Editorial Luxe (Fraunces/Manrope, paper+ink+oxblood, hard offset shadows); its one `backdrop-blur` is incidental. `backend-node-serverless` — that skill is Lambda/Serverless-Framework scoped; `server.mjs` is a plain Express process with no `serverless.yml` or `aws-lambda`. `aws-platform`, `firebase-platform`, `data-postgres-dynamo` and both payments skills — no such dependency in `package.json` or `src/`. Editing `server.mjs` has no matching tech skill: treat it as plain Node + Express, consulting expressjs.com.
